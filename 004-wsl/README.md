```bash
# Instalar WLS no cmd
> wsl --install

#  Instalar g++ no linux
> apt update
> apt install g++

#   Quando for compilar, explicitar .out no arquivo gerado para
> g++ -Wall 002-lista-linguagem-cpp/ex03/programa.cpp -o programa.out
#  Rodar .sh
#   Tornar o .sh rodavel
> chmod +x arquivo.sh
#   Executar arquivo(lembrando que linux tem que explicitar o .out no g++)
> .\arquvo.out
```